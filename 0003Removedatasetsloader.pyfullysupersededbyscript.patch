From d5e03ad5868a2cf02822dbeee38ca5e704ecbde3 Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Tue, 8 Sep 2026 13:56:50 +0000
Subject: [PATCH 3/3] Remove datasets/loader.py, fully superseded by
 scripts/check_env.py

Carried forward from PHASE-1.md #3 ("decide whether to keep or remove").
No diff against its last commit, not imported anywhere, and check_env.py
already does everything it did plus GC10-DET/DeepCrack/SDNET2018 auditing.
---
 datasets/loader.py | 31 -------------------------------
 1 file changed, 31 deletions(-)
 delete mode 100644 datasets/loader.py

diff --git a/datasets/loader.py b/datasets/loader.py
deleted file mode 100644
index fa72e265..00000000
--- a/datasets/loader.py
+++ /dev/null
@@ -1,31 +0,0 @@
-from pathlib import Path
-
-
-class DatasetLoader:
-    def __init__(self, dataset_root="."):
-        self.dataset_root = Path(dataset_root)
-
-        self.datasets = {
-            "NEU-DET": self.dataset_root / "NEU-DET",
-            "SDNET2018": self.dataset_root / "SDNET2018"
-        }
-
-    def verify_datasets(self):
-        print("=" * 50)
-        print("Dataset Verification")
-        print("=" * 50)
-
-        for name, path in self.datasets.items():
-            if path.exists():
-                num_files = sum(1 for file in path.rglob("*") if file.is_file())
-                print(f"{name:<12} : Found")
-                print(f"Location     : {path.resolve()}")
-                print(f"Total Files  : {num_files}\n")
-            else:
-                print(f"{name:<12} : Not Found")
-                print(f"Expected     : {path.resolve()}\n")
-
-
-if __name__ == "__main__":
-    loader = DatasetLoader()
-    loader.verify_datasets()
\ No newline at end of file
-- 
2.43.0

